/** No design exists yet for this tab — placeholder stub so the nav destination is wired up. */
export function AnalysisScreen() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-3 px-6 text-center">
      <i className="ph-bold ph-chart-bar" style={{ fontSize: 40, color: "rgba(0,0,0,.3)" }} />
      <div style={{ font: "800 17px Archivo" }}>Analysis</div>
      <div style={{ font: "500 13px Archivo", color: "rgba(0,0,0,.55)" }}>Coming soon.</div>
    </div>
  );
}
